# GEMINI.md

Read `AGENTS.md` first; it is the shared entry point for every AI model on this project. A rebuild is in progress: `godot/docs/REBUILD-PLAN.md` is the source of truth and `godot/docs/REBUILD-LOG.md` is the shared progress log. Follow the plan's working protocol (section 9): claim a task in the log, work on an `rb/<task-id>-<slug>` branch, run the listed checks, read stderr, and log the result.
