# Particle window captures

These three windowed captures come from the `--shots` mode in `tests/v2/particles.gd` using the
Kenney CC0 atlas: tyre smoke (`smoke.png`), gravel (`gravel.png`) and wall-contact sparks
(`sparks.png`). The capture scene is a simple 3D ground plane and camera for visual review.
